/*
Buscar un elemento dentro de un arreglo que nosotros le pedimos por teclado.
Indicar las posición donde se encuentra. Si hay más de uno, indicar
igualmente la posición. (FindIndex)
*/

let numero1 = parseInt(prompt("digite el numero"));
numb1 = numero1;
function numero() {
  if ((numero = numb1)) {
    let numb1 = [4, 5, 8, 9, 5, 4, 5, 8, 9, 1, 1, 1];
    let cantidad = numb1.indexOf((Element = numero1));
    console.log("se encuentra en la pocision: " + cantidad);
  }
}
numero();
